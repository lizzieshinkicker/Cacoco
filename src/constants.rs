/// The standard virtual width of the Doom internal coordinate system.
pub const DOOM_W: f32 = 320.0;

/// The standard virtual height of the Doom internal coordinate system.
pub const DOOM_H: f32 = 200.0;

/// The virtual width of a 16:9 widescreen canvas.
pub const DOOM_W_WIDE: f32 = 428.0;

/// The internal logic frequency of the Doom engine (Tics per second).
pub const DOOM_TICS_PER_SEC: f64 = 35.0;

/// Default fallback width for a character if the texture is missing.
pub const DEFAULT_GLYPH_W: f32 = 8.0;

/// Default fallback height for a character if the texture is missing.
pub const DEFAULT_GLYPH_H: f32 = 8.0;
